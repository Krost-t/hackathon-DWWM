<!DOCTYPE html>
<html>
    <head>
        <!-- Configuration de base du document HTML -->
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {# Font Awesome for icons #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        
        <!-- Inclusion des fichiers CSS -->
        {% block stylesheets %}

        <!-- Bootstrap CSS pour le responsive design -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
        <!-- Font Awesome pour les icônes -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        <link rel="stylesheet" href="{{ asset('/styles/app.css') }}">
        <link rel="stylesheet" href="{{ asset('/styles/base.css') }}">

        {% endblock %}

        <!-- Inclusion des fichiers JavaScript -->
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    
    <!-- Corps principal de la page -->
    <body>
        <div class="top-promo-bar">
            <p><strong>Special Offer:</strong> Free Shipping on all the orders above $100</p>
            <button class="close-promo-btn" aria-label="Fermer l'offre spéciale"><i class="fas fa-times"></i></button>
        </div>

        <header class="stylish-header">
            <div class="header-left-stylish">
                <div class="social-icons">
                    <a href="#"><i class="fab fa-facebook-f"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-youtube"></i></a>
                </div>
                <div class="store-brand">
                    <h1>Stylish</h1>
                    <p>Online Store</p>
                </div>
            </div>
            <div class="header-right-stylish">
                <div class="search-bar-stylish">
                    <input type="text" placeholder="" aria-label="Rechercher des produits">
                    <button aria-label="Rechercher"><i class="fas fa-search"></i></button>
                </div>
                <a href="#" class="auth-btn">Connection</a>
                <a href="#" class="auth-btn">S'enregistrer</a>
            </div>
        </header>

        <nav class="main-nav-stylish">
            <ul class="nav-list-stylish">
                <li><a href="/accueil" class="nav-link-stylish">Accueil</a></li>
                <li><a href="/produits" class="nav-link-stylish">Nos articles</a></li>
                <li><a href="#" class="nav-link-stylish">Blog</a></li>
                <li><a href="#" class="nav-link-stylish">Contact</a></li>
                <li><button class="return-article-btn">Retour d'article</button></li>
            </ul>
        </nav>
        {% block body %}{% endblock %}

        <!-- Footer Section -->
        <footer class="main-footer">
            <div class="footer-section">
                <h3>Info</h3>
                <ul>
                    <li><a href="#">Track Your Order</a></li>
                    <li><a href="#">Our Blog</a></li>
                    <li><a href="#">Privacy policy</a></li>
                    <li><a href="#">Shipping</a></li>
                    <li><a href="#">Contact Us</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>About</h3>
                <ul>
                    <li><a href="#">History</a></li>
                    <li><a href="#">Our Team</a></li>
                    <li><a href="#">Services</a></li>
                    <li><a href="#">Company</a></li>
                    <li><a href="#">Manufacture</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Women Shoes</h3>
                <ul>
                    <li><a href="#">Track Your Order</a></li>
                    <li><a href="#">Our Blog</a></li>
                    <li><a href="#">Privacy policy</a></li>
                    <li><a href="#">Shipping</a></li>
                    <li><a href="#">Contact Us</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Popular</h3>
                <ul>
                    <li><a href="#">Prices Drop</a></li>
                    <li><a href="#">New Products</a></li>
                    <li><a href="#">Best Sales</a></li>
                    <li><a href="#">Stores</a></li>
                    <li><a href="#">Contact Us</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Mens Collection</h3>
                <ul>
                    <li><a href="#">Delivery</a></li>
                    <li><a href="#">About Us</a></li>
                    <li><a href="#">Shoes</a></li>
                    <li><a href="#">Contact Us</a></li>
                </ul>
            </div>
        </footer>

        <style>
            /* Basic CSS Reset & Global Styles */
            :root {
                --nike-black: #111;
                --nike-grey: #f6f6f6;
                --nike-light-grey: #707070;
                --nike-white: #fff;
                --nike-font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; /* System font stack */
                --spacing-sm: 0.5rem;
                --spacing-md: 1rem;
                --spacing-lg: 2rem;
                --spacing-xl: 3rem;
                --nike-border-radius: 5px;
                --nike-red: #ff0000;
                --nike-dark-grey: #333;
            }
            body {
                margin: 0;
                font-family: var(--nike-font-family);
                color: var(--nike-black);
                background-color: var(--nike-white);
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }
            *, *::before, *::after {
                box-sizing: border-box;
            }
            a {
                text-decoration: none;
                color: inherit;
            }
            button {
                border: none;
                background: none;
                cursor: pointer;
                padding: 0;
            }

            /* Header & Navbar Styles from produit/index.html.twig */
            .top-promo-bar {
                background-color: #f2f2f2;
                color: #333;
                padding: 8px 20px;
                text-align: center;
                font-size: 0.9em;
                display: flex;
                justify-content: center;
                align-items: center;
                position: relative;
            }

            .close-promo-btn {
                background: none;
                border: none;
                color: #666;
                font-size: 1em;
                cursor: pointer;
                position: absolute;
                right: 20px;
            }

            .stylish-header {
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 10px 20px;
                border-bottom: 1px solid #eee;
            }

            .header-left-stylish {
                display: flex;
                align-items: center;
                gap: 20px;
            }

            .social-icons a {
                color: #333;
                margin-right: 10px;
                font-size: 1.2em;
            }

            .store-brand h1 {
                font-family: 'Times New Roman', serif; /* Assuming a serif font for "Stylish" */
                font-size: 2.2em;
                margin: 0;
                line-height: 1;
            }

            .store-brand p {
                font-size: 0.8em;
                margin: 0;
                text-transform: uppercase;
                letter-spacing: 2px;
            }

            .header-right-stylish {
                display: flex;
                align-items: center;
                gap: 15px;
            }

            .search-bar-stylish {
                display: flex;
                border: 1px solid #ccc;
                border-radius: 20px;
                overflow: hidden;
                width: 250px;
            }

            .search-bar-stylish input {
                border: none;
                padding: 8px 15px;
                flex-grow: 1;
                outline: none;
            }

            .search-bar-stylish button {
                background-color: #fff;
                border: none;
                padding: 8px 15px;
                cursor: pointer;
                font-size: 1em;
                color: #555;
            }

            .auth-btn {
                background-color: #333;
                color: #fff;
                padding: 8px 15px;
                border-radius: 20px;
                text-decoration: none;
                font-size: 0.9em;
            }

            .main-nav-stylish {
                background-color: #111; /* Dark background for navigation bar */
                padding: 10px 20px;
            }

            .nav-list-stylish {
                list-style: none;
                margin: 0;
                padding: 0;
                display: flex;
                justify-content: center;
                gap: 30px;
            }

            .nav-link-stylish {
                color: #fff;
                text-decoration: none;
                font-weight: bold;
                padding: 5px 0;
                position: relative;
            }

            .nav-link-stylish::after {
                content: '';
                position: absolute;
                left: 0;
                bottom: 0;
                width: 0;
                height: 2px;
                background-color: #fff;
                transition: width 0.3s ease;
            }

            .nav-link-stylish:hover::after {
                width: 100%;
            }

            .return-article-btn {
                background-color: #fff;
                color: #111;
                padding: 8px 15px;
                border-radius: 20px;
                font-size: 0.9em;
                font-weight: bold;
            }

            /* Responsive Header */
            @media (max-width: 992px) {
                .main-nav {
                    display: none; /* Hide navigation on smaller screens for simplicity */
                }
                .main-header {
                    padding: var(--spacing-md);
                }
                .search-input {
                    width: 150px;
                }
            }

            @media (max-width: 768px) {
                .main-header {
                    flex-wrap: wrap;
                    justify-content: center;
                    gap: var(--spacing-md);
                }
                .header-left, .header-center, .header-right {
                    flex-basis: 100%;
                    display: flex;
                    justify-content: center;
                }
                .search-input {
                    width: 100%;
                    max-width: 250px;
                }
            }

            /* Footer Styles */
            .main-footer {
                background-color: var(--nike-black);
                color: var(--nike-white);
                padding: var(--spacing-xl);
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
                gap: var(--spacing-lg);
                position: relative;
                bottom: 0;
                width: 100%;
            }

            .footer-section h3 {
                font-size: 1.1rem;
                margin-bottom: var(--spacing-md);
                text-transform: uppercase;
                letter-spacing: 0.5px;
                color: var(--nike-white);
            }

            .footer-section ul {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .footer-section ul li a {
                display: block;
                padding: 5px 0;
                color: rgba(255, 255, 255, 0.7);
                text-decoration: none;
                transition: color 0.2s ease;
            }

            .footer-section ul li a:hover {
                color: var(--nike-white);
            }

            .footer-section address {
                font-style: normal;
                color: rgba(255, 255, 255, 0.7);
                line-height: 1.6;
            }

            /* Responsive Footer */
            @media (max-width: 768px) {
                .main-footer {
                    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
                    padding: var(--spacing-lg);
                }
            }

            @media (max-width: 480px) {
                .main-footer {
                    grid-template-columns: 1fr;
                    text-align: center;
                }
            }

            /* Specific styles for the product listing page (originally from produit/index.html.twig) */

            .product-listing-main {
                display: flex;
                padding: 20px;
                max-width: 1400px;
                margin: 0 auto;
            }

            .filter-sidebar {
                flex: 0 0 280px;
                margin-right: 30px;
                padding: 20px;
                background-color: #f8f8f8;
                border-radius: 8px;
                box-shadow: 0 2px 5px rgba(0,0,0,0.05);
                position: sticky;
                top: 20px;
                height: fit-content;
            }

            .filter-header {
                display: flex;
                justify-content: space-between;
                align-items: center;
                margin-bottom: 20px;
                padding-bottom: 15px;
                border-bottom: 1px solid #eee;
            }

            .filter-header h2 {
                font-size: 1.5rem;
                margin: 0;
                color: #333;
            }

            .clear-filters-btn {
                background: none;
                border: none;
                color: #888;
                font-size: 0.9em;
                cursor: pointer;
                display: flex;
                align-items: center;
                gap: 5px;
            }

            .clear-filters-btn:hover {
                color: #333;
            }

            .filter-section {
                margin-bottom: 20px;
                border-bottom: 1px solid #eee;
                padding-bottom: 15px;
            }

            .filter-section:last-child {
                border-bottom: none;
                margin-bottom: 0;
                padding-bottom: 0;
            }

            .filter-title {
                font-size: 1.1rem;
                font-weight: 600;
                margin-bottom: 10px;
                color: #333;
            }

            .filter-options {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .filter-options li {
                margin-bottom: 8px;
            }

            .filter-link {
                color: #555;
                text-decoration: none;
                transition: color 0.2s ease;
            }

            .filter-link:hover {
                color: #000;
            }

            .filter-section.expandable .filter-title {
                cursor: pointer;
                display: flex;
                justify-content: space-between;
                align-items: center;
                width: 100%;
                padding: 5px 0;
            }

            .filter-section.expandable .filter-title i {
                transition: transform 0.3s ease;
            }

            .filter-section.expandable .filter-title[aria-expanded="false"] i {
                transform: rotate(-90deg);
            }

            .filter-options.expandable-content {
                overflow: hidden;
                transition: max-height 0.3s ease-out;
                max-height: 500px; /* Grande valeur pour le défilement */
            }

            .filter-options.expandable-content[aria-hidden="true"] {
                max-height: 0;
            }

            .checkbox-container {
                display: block;
                position: relative;
                padding-left: 25px;
                margin-bottom: 8px;
                cursor: pointer;
                font-size: 0.95em;
                -webkit-user-select: none;
                -moz-user-select: none;
                -ms-user-select: none;
                user-select: none;
                color: #555;
            }

            .checkbox-container input {
                position: absolute;
                opacity: 0;
                cursor: pointer;
                height: 0;
                width: 0;
            }

            .checkmark {
                position: absolute;
                top: 2px;
                left: 0;
                height: 18px;
                width: 18px;
                background-color: #eee;
                border-radius: 3px;
            }

            .checkbox-container:hover input ~ .checkmark {
                background-color: #ccc;
            }

            .checkbox-container input:checked ~ .checkmark {
                background-color: #111;
            }

            .checkmark:after {
                content: "";
                position: absolute;
                display: none;
            }

            .checkbox-container input:checked ~ .checkmark:after {
                display: block;
            }

            .checkbox-container .checkmark:after {
                left: 6px;
                top: 2px;
                width: 5px;
                height: 10px;
                border: solid white;
                border-width: 0 2px 2px 0;
                -webkit-transform: rotate(45deg);
                -ms-transform: rotate(45deg);
                transform: rotate(45deg);
            }

            .size-grid {
                display: grid;
                grid-template-columns: repeat(3, 1fr); /* 3 columns for sizes */
                gap: 5px;
            }

            .size-checkbox {
                display: flex;
                justify-content: center;
                align-items: center;
                border: 1px solid #ddd;
                border-radius: 4px;
                padding: 8px;
                cursor: pointer;
                font-size: 0.9em;
                transition: all 0.2s ease;
                color: #555;
            }

            .size-checkbox input {
                display: none;
            }

            .size-checkbox .size-label {
                user-select: none;
            }

            .size-checkbox:hover {
                border-color: #888;
            }

            .size-checkbox input:checked + .size-label {
                background-color: #111;
                color: #fff;
                border-color: #111;
            }

            .color-grid {
                display: grid;
                grid-template-columns: repeat(4, 1fr); /* 4 columns for colors */
                gap: 8px;
            }

            .color-checkbox {
                display: flex;
                flex-direction: column;
                align-items: center;
                cursor: pointer;
            }

            .color-checkbox input {
                display: none;
            }

            .color-label {
                width: 30px;
                height: 30px;
                border-radius: 50%;
                border: 2px solid #ddd; /* Default border */
                margin-bottom: 5px;
                transition: border-color 0.2s ease;
            }

            .color-checkbox input:checked ~ .color-label {
                border-color: #111; /* Highlight selected color */
                box-shadow: 0 0 0 2px #111; /* Outer ring for selection */
            }

            .color-name {
                font-size: 0.8em;
                color: #555;
            }

            .product-listing-content {
                flex-grow: 1;
            }

            .product-listing-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 20px;
            }

            .product-card {
                background-color: #f9f9f9;
                border-radius: 8px;
                overflow: hidden;
                box-shadow: 0 2px 5px rgba(0,0,0,0.05);
                text-align: left;
                transition: transform 0.2s ease, box-shadow 0.2s ease;
                position: relative;
                display: flex;
                flex-direction: column;
            }

            .product-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 15px rgba(0,0,0,0.1);
            }

            .product-card-image-wrapper {
                width: 100%;
                padding-bottom: 75%; /* 4:3 Aspect Ratio */
                position: relative;
                overflow: hidden;
            }

            .product-card-image-wrapper img {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                object-fit: cover;
                background-color: #eee;
            }

            .product-card-details {
                padding: 15px;
                flex-grow: 1;
            }

            .product-card-title {
                font-size: 1.1em;
                font-weight: 600;
                margin-bottom: 5px;
                color: #333;
            }

            .product-card-price {
                font-size: 1em;
                color: #666;
                margin-bottom: 10px;
            }

            .product-card-actions {
                padding: 15px;
                border-top: 1px solid #eee;
                text-align: right;
            }

            .add-to-cart-button {
                background-color: #111;
                color: #fff;
                padding: 10px 15px;
                border-radius: 20px;
                font-size: 0.9em;
                font-weight: bold;
                text-transform: uppercase;
                cursor: pointer;
                transition: background-color 0.2s ease;
            }

            .add-to-cart-button:hover {
                background-color: #555;
            }

            .promotion-badge {
                position: absolute;
                top: 10px;
                left: 10px;
                background-color: red;
                color: white;
                padding: 5px 10px;
                border-radius: 5px;
                font-size: 0.8em;
                font-weight: bold;
                z-index: 10;
            }

            /* Responsive adjustments for product listing */
            @media (max-width: 992px) {
                .product-listing-main {
                    flex-direction: column;
                    padding: 15px;
                }

                .filter-sidebar {
                    flex: none;
                    width: 100%;
                    margin-right: 0;
                    margin-bottom: 20px;
                    position: static;
                    top: auto;
                }

                .product-listing-grid {
                    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                    gap: 15px;
                }
            }

            @media (max-width: 768px) {
                .stylish-header {
                    flex-direction: column;
                    align-items: center;
                    gap: 10px;
                }
                .header-left-stylish, .header-right-stylish {
                    flex-direction: column;
                    align-items: center;
                    gap: 10px;
                    width: 100%;
                }
                .main-nav-stylish {
                    padding: 8px 10px;
                }
                .nav-list-stylish {
                    flex-wrap: wrap;
                    gap: 15px;
                }
                .search-bar-stylish {
                    width: 100%;
                    max-width: 300px;
                }
                .auth-btn {
                    width: 100%;
                    text-align: center;
                }
                .product-card {
                    padding: 10px;
                }
                .product-card-details {
                    padding: 10px;
                }
                .product-card-actions {
                    padding: 10px;
                }
            }

            @media (max-width: 576px) {
                .top-promo-bar {
                    padding: 8px 10px;
                    font-size: 0.8em;
                }
                .stylish-header {
                    padding: 10px;
                }
                .store-brand h1 {
                    font-size: 1.8em;
                }
                .filter-sidebar {
                    padding: 15px;
                }
                .product-listing-grid {
                    grid-template-columns: 1fr; /* Single column on very small screens */
                }
            }

            /* Styles for product detail page */
            .product-page-detail {
                max-width: 1200px;
                margin: 20px auto;
                padding: 0 20px;
            }

            .product-detail-container {
                display: flex;
                gap: 40px;
            }

            .product-gallery-detail {
                flex: 1;
            }

            .gallery-main-detail {
                position: relative;
                margin-bottom: 10px;
                border: 1px solid #e0e0e0;
                border-radius: 8px;
                overflow: hidden;
            }

            .gallery-main-detail img {
                width: 100%;
                height: auto;
                display: block;
            }

            .gallery-thumbnails-detail {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
                gap: 10px;
            }

            .thumbnail-btn-detail {
                background: none;
                border: 1px solid #e0e0e0;
                border-radius: 8px;
                padding: 5px;
                cursor: pointer;
                overflow: hidden;
            }

            .thumbnail-btn-detail img {
                width: 100%;
                height: auto;
                display: block;
                border-radius: 5px;
            }

            .thumbnail-btn-detail.active, .thumbnail-btn-detail:hover {
                border-color: #111;
            }

            .product-details-info {
                flex: 1;
            }

            .product-title-detail {
                font-size: 2.5rem;
                font-weight: 700;
                margin-top: 0;
                margin-bottom: 10px;
                color: #111;
            }

            .product-price-detail {
                font-size: 1.8rem;
                font-weight: 600;
                color: #333;
                margin-bottom: 20px;
            }

            .product-attributes {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
                gap: 15px;
                margin-bottom: 30px;
            }

            .attribute-group h3 {
                font-size: 1em;
                color: #707070;
                margin-bottom: 5px;
                text-transform: uppercase;
            }

            .attribute-group p {
                font-size: 1.1em;
                font-weight: 500;
                color: #111;
                margin: 0;
            }

            .product-description-detail {
                margin-bottom: 30px;
            }

            .product-description-detail h2 {
                font-size: 1.5rem;
                margin-bottom: 10px;
            }

            .product-description-detail p {
                font-size: 1em;
                line-height: 1.6;
                color: #555;
            }

            .size-selector h3 {
                font-size: 1em;
                margin-bottom: 10px;
                color: #707070;
                text-transform: uppercase;
            }

            .size-selector .size-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(60px, 1fr));
                gap: 10px;
                margin-bottom: 20px;
            }

            .add-to-cart-btn-detail {
                background-color: #111;
                color: #fff;
                padding: 15px 30px;
                font-size: 1.1rem;
                font-weight: bold;
                border-radius: 30px;
                width: 100%;
                display: flex;
                justify-content: center;
                align-items: center;
                gap: 10px;
                transition: background-color 0.2s ease;
            }

            .add-to-cart-btn-detail:hover {
                background-color: #555;
            }

            .add-to-cart-btn-detail i {
                font-size: 1.2em;
            }

            /* Responsive for product detail page */
            @media (max-width: 992px) {
                .product-detail-container {
                    flex-direction: column;
                    gap: 20px;
                }
                .product-page-detail {
                    padding: 0 15px;
                }
            }

            @media (max-width: 576px) {
                .product-title-detail {
                    font-size: 2rem;
                }
                .product-price-detail {
                    font-size: 1.5rem;
                }
                .product-attributes {
                    grid-template-columns: 1fr;
                }
                .size-selector .size-grid {
                    grid-template-columns: repeat(auto-fit, minmax(50px, 1fr));
                }
                .add-to-cart-btn-detail {
                    padding: 12px 20px;
                    font-size: 1rem;
                }
            }
        </style>
    </body>
</html>